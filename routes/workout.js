const { application } = require("express");

getLastWorkout();
app.get();

addExercise(data);
app.post();

createWorkout((data = {}));
app.post();
db.workout.insert;

getWorkoutsInRange();
app.get();
db.workout.find();

module.exports = router;
